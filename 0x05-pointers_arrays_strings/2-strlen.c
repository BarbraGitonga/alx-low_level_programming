#include "main.h"

/**
 *_strlen - returns length of a string
 *@s: sting to count length
 *
 *Return: length of string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		int len;

		len = i;
	}
	return (i + 1);
}
